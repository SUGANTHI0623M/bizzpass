# Database (for Docker use postgres as DB_HOST; for production use your DB host)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bizzpass
DB_USER=dev
DB_PASSWORD=dev1234

# JWT
JWT_SECRET=shbizzpass
JWT_EXPIRE_DAYS=30

# Production deployment - set these for non-Docker deployments (K8s, VM, etc.)
# CRM_BACKEND_URL=https://api.yourdomain.com
# FRONTEND_URL=https://app.yourdomain.com
# Used for Paysharp webhook/return URLs in subscription flow
CRM_BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:8080

# Integrations encryption (optional; falls back to JWT_SECRET if not set)
# INTEGRATION_ENCRYPTION_KEY=

# Paysharp (optional - overrides DB config when set; for local testing)
# PAYSHARP_API_KEY=your-api-token
# PAYSHARP_SECRET=your-secret
# PAYSHARP_MERCHANT_ID=your-merchant-id
# PAYSHARP_SANDBOX=false

# Payment testing: override gateway amount when set. Set to 0 for production (actual plan amount).
# Paysharp requires minimum ₹10 (1000 paise); amounts below are auto-raised to ₹10.
PAYMENT_TEST_AMOUNT_PAISE=1000

# Razorpay (optional - for Card/Netbanking/Wallet; add in Integrations or here)
# RAZORPAY_KEY_ID=rzp_test_xxxx
# RAZORPAY_KEY_SECRET=your-secret

# Cloudinary
CLOUDINARY_CLOUD_NAME=dyi7xoqhy
CLOUDINARY_API_KEY=587679965546116
CLOUDINARY_API_SECRET=SnmyCwCyL1rjiMMGlmOeR7kHttI
